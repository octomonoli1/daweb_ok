spring:
  application:
    name: daweb
  datasource:
    url: jdbc:mysql://localhost:3307/daweb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # crea o actualiza tablas automáticamente (solo en desarrollo)
    show-sql: true      # muestra las consultas SQL en consola
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
